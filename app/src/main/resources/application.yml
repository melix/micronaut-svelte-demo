micronaut:
  application:
    name: app
  router:
    static-resources:
      default:
        enabled: true
        mapping: "/**"
        paths: classpath:static
netty:
  default:
    allocator:
      max-order: 3

